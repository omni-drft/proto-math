/**
* @file pmEnum.h
* @brief This files contains only the enum pmEnum.
* 
* This file defines the enum pmEnum, which contains various flags that are used across the library.
*/

/// <summary>
/// The enum pmEnum is used to define different parameters across the library.
/// 
/// PM_PRINT_DEFAULT: The default print mode, which prints the results in a table format.
/// PM_PRINT_SIMPLEST: The simplest print mode, which prints the results in a simple format.
/// PM_PRINT_POLAR: The polar print mode, which prints the results in a polar format.
///
/// </summary>
enum pmEnum {
	PM_PRINT_DEFAULT,
	PM_PRINT_SIMPLEST,
	PM_PRINT_POLAR
};